import 'package:flutter/material.dart';

class TeamStatusBadge extends StatelessWidget {
  const TeamStatusBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      child: Text('TeamStatusBadge'),
    );
  }
}
