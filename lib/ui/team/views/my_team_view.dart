import 'package:flutter/material.dart';

class MyTeamView extends StatelessWidget {
  const MyTeamView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('MyTeamView'),
      ),
      body: const Center(
        child: Text('MyTeamView'),
      ),
    );
  }
}
