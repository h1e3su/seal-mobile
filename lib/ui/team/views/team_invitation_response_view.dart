import 'package:flutter/material.dart';

class TeamInvitationResponseView extends StatelessWidget {
  const TeamInvitationResponseView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('TeamInvitationResponseView'),
      ),
      body: const Center(
        child: Text('TeamInvitationResponseView'),
      ),
    );
  }
}
