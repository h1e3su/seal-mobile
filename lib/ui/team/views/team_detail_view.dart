import 'package:flutter/material.dart';

class TeamDetailView extends StatelessWidget {
  const TeamDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('TeamDetailView'),
      ),
      body: const Center(
        child: Text('TeamDetailView'),
      ),
    );
  }
}
