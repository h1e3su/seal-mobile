import 'package:flutter/material.dart';

class TrackChip extends StatelessWidget {
  const TrackChip({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      child: Text('TrackChip'),
    );
  }
}
