import 'package:flutter/material.dart';

class StudentProfileSetupView extends StatelessWidget {
  const StudentProfileSetupView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('StudentProfileSetupView'),
      ),
      body: const Center(
        child: Text('StudentProfileSetupView'),
      ),
    );
  }
}
